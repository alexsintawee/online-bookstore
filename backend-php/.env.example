APP_NAME="Online Bookstore API"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:5555

LOG_CHANNEL=stack
LOG_LEVEL=debug

# Data directory is resolved as ../data relative to this app (project root's data folder).
# Run: php artisan serve --port=5555
